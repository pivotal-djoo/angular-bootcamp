---
spring:
  application:
    name: angular-bootcamp-api
  jpa:
    database: mysql
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://localhost:3306/angular-bootcamp-db?useSSL=false
    username: bootcamper
    password: password
server:
  port: 8181
